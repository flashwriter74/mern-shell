{
  "name": "orbit_applications",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": "12.x"
  },
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd creditapp && npm run build",
    "install-client": "cd creditapp && npm install",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "heroku-postbuild": "npm run install-client && npm run build"
  },
  "keywords": [],
  "author": "Joe Polidoro",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "body-parser": "^1.20.1",
    "concurrently": "^5.2.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^8.5.1",
    "mariadb": "^2.4.0",
    "mysql2": "^2.1.0",
    "sequelize": "^5.21.11"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
